name: scheduler
description: Scheduler from Duda.

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^ 6.1.1
  equatable: ^ 1.0.0
  sqflite: ^ 1.3.0
  path_provider: ^ 1.6.24
  percent_indicator: ^2.1.7+2
  flutter_launcher_icons: ^0.7.4


  cupertino_icons: ^0.1.3

flutter_icons:
  image_path: "icon/icon.png"
  android: true
  ios: true

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true

